#include <gtest/gtest.h>
#include <iostream>
#include <random>

//#include <integrate.hpp>

TEST(ExplicitScheme1D, Amatrix)
{
//ExplicitScheme1D scheme();
//ASSERT_EQ(grid.get_deltax(), 0.0);
//ASSERT_EQ(grid.get_state().size(), 0);
std::cout << "ciao";
}
